name: Build Android App Bundle
run-name: Build Celestial Oracle Android App 🚀

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build Android App Bundle
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3. Setup Java
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      # 4. Setup Android SDK
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # 5. Install dependencies
      - name: Install dependencies
        run: npm install

      # 6. Build Vite app
      - name: Build Vite app
        run: npm run build

      # 7. Add Android platform
      - name: Add Android platform
        run: |
          npx cap add android || echo "Android already added"
          npx cap sync android

      # 8. Update API Levels to 35
      - name: Update API Levels
        run: |
          BUILD_GRADLE="android/app/build.gradle"
          
          # Update compileSdkVersion to 35
          sed -i 's/compileSdkVersion = .*/compileSdkVersion = 35/g' "$BUILD_GRADLE"
          
          # Update targetSdkVersion to 35
          sed -i 's/targetSdkVersion = .*/targetSdkVersion = 35/g' "$BUILD_GRADLE"
          
          echo "✅ API levels updated to 35"
          grep -i "sdkversion" "$BUILD_GRADLE" || echo "Checking build.gradle..."

      # 8.5. Update Version Code to 2
      - name: Update Version Code
        run: |
          BUILD_GRADLE="android/app/build.gradle"
          
          # Update versionCode to 2
          sed -i 's/versionCode = .*/versionCode = 2/g' "$BUILD_GRADLE"
          
          echo "✅ Version code updated to 2"

      # 9. Decode and setup keystore
      - name: Decode Keystore
        run: |
          mkdir -p android/app
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/app/keystore.jks
          ls -lh android/app/keystore.jks

      # 10. Make gradlew executable
      - name: Make gradlew executable
        run: chmod +x android/gradlew

      # 11. Build App Bundle (AAB)
      - name: Build App Bundle
        run: |
          cd android
          ./gradlew bundleRelease \
            -Pandroid.injected.signing.store.file=$(pwd)/app/keystore.jks \
            -Pandroid.injected.signing.store.password="${{ secrets.KEYSTORE_PASSWORD }}" \
            -Pandroid.injected.signing.key.alias="${{ secrets.KEY_ALIAS }}" \
            -Pandroid.injected.signing.key.password="${{ secrets.KEY_PASSWORD }}" \
            --stacktrace

      # 12. Upload App Bundle as artifact
      - name: Upload App Bundle
        uses: actions/upload-artifact@v4
        with:
          name: celestial-oracle-release-aab
          path: android/app/build/outputs/bundle/release/app-release.aab
          retention-days: 30

      # 13. Display success message
      - name: Build Success
        run: echo "✅ App Bundle built successfully! Download from artifacts."
